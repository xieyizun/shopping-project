<div id="show_orders"><%= render current_order %></div>
<div id="paynow">
	<% if current_order.items.count > 0 %>
  		<div class="row">
  			<%= link_to "Pay Now", new_payment_path %>
  		</div>
	<% end %>
</div>
<div class="row">
  	<%= link_to "Continue Shopping", root_url %>
</div>
</br>
